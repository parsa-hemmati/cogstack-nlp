services:
  anoncat:
    build:
      context: .
      dockerfile: Dockerfile
    command: ./scripts/run.sh
    env_file:
      - ./app/.env
    volumes:
      - ../medcat-models:/home/models
      - app-db:/home/app/db
    restart: unless-stopped
    ports:
      - "8003:8000"

volumes:
  app-db:
